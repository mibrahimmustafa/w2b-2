# Main Libraries
requests==2.32.5
beautifulsoup4==4.13.4
scrapling==0.4.2
ddgs>=7.0.0   # Official DDGS API — avoids CAPTCHA / IP blocks


# Engines required by Scrapling for AsyncStealthySession
curl_cffi==0.14.0
playwright==1.58.0
patchright==1.58.2
browserforge>=1.1.0
msgspec>=0.18.6

# Parsers used by BeautifulSoup and Scrapling's internal logic
lxml==6.0.2

# API / Web App
fastapi==0.115.0
uvicorn==0.30.0
# pydantic==2.9.0
python-multipart==0.0.9

# Vector DB / RAG
chromadb>=1.5.0
sentence-transformers==2.7.0
